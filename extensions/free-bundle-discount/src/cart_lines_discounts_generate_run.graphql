query CartInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          id
        }
      }
    }
  }
  discount {
    discountClasses
  }
  shop {
    metafield(namespace: "custom", key: "bundle_config") {
      jsonValue
    }
  }
}
